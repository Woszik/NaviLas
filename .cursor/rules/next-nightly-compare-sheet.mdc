---
description: Porównaj is an overlay card on the list (0.5.65), not a BottomSheet
globs: app/src/main/res/layout/page_list.xml
alwaysApply: false
---

# Porównaj — karta overlay, nie sheet

Od Nightly **0.5.65** tabela jest w `compareOverlay` na ekranie listy (X / wstecz). Nie wracać do `BottomSheetDialog` ani do nagłówka listy. Compact i D odłożone.
